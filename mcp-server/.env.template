# =============================================================================
# WhatsApp MCP Server - Environment Template
# =============================================================================

# --- Server Configuration ---
PORT=3000
LOG_LEVEL=info # debug, info, warn, error

# --- Database Configuration (Prisma) ---
# Set to 'true' to use PostgreSQL/Supabase instead of filesystem for storage
USE_DATABASE=false

# Connect to Supabase via connection pooling (e.g., PgBouncer)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true
DATABASE_URL="postgresql://user:password@host:6543/postgres?pgbouncer=true"

# Direct connection to the database (bypasses PgBouncer)
# Required for running prisma migrations (npx prisma migrate dev)
DIRECT_URL="postgresql://user:password@host:5432/postgres"

# --- WhatsApp Configuration ---
# Directory where authentication state will be stored if NOT using database
AUTH_DIR="./auth_info"

# --- Webhook Configuration (Optional) ---
# Secret for signing webhook payloads
WEBHOOK_SECRET="your_webhook_secret_here"

# --- HTTP Server Configuration ---
# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS="*"
